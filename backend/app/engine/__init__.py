"""The orchestration engine. Domain-agnostic: it coordinates agents and never
embeds domain knowledge (that lives in agent YAML).

Importable and runnable without the web server — see `graph_builder.build` and
`scripts/run_crew.py`.
"""
